<%= form_for(master_variant) do |f| %>
  <% if master_variant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(master_variant.errors.count, "error") %> prohibited this master_variant from being saved:</h2>

      <ul>
      <% master_variant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <div class="field">
       <%=f.label :variant%>
       <%=f.text_field :variant%>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
