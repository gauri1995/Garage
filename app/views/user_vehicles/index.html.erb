<h1>User Vehicles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_vehicles.each do |user_vehicle| %>
      <tr>
      <td><%=user_vehicle.vehicle.car.name%></td>
       <td><%=user_vehicle.vehicle.master_variant.variant%></td>
        <td><%= link_to 'Show', user_vehicle %></td>
        <td><%= link_to 'Edit', edit_user_vehicle_path(user_vehicle) %></td>
        <td><%= link_to 'Destroy', user_vehicle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User Vehicle', new_user_vehicle_path %>
